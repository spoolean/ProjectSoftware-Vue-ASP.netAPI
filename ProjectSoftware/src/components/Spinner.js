import { BSpinner } from "bootstrap-vue"

export default {
    name: "Spinner",
    render() {
        return (
            <div>
                <BSpinner></BSpinner>
            </div>
        );
    }
};
